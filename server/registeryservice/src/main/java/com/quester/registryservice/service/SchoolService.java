package com.quester.registryservice.service;


import org.springframework.beans.factory.annotation.Autowired;

public class SchoolService {
    /*@Autowired
    SchoolRepository repository;
    public School registerSchool(SchoolGroup schoolGroup,String schoolName){
        School school = new School(schoolName, schoolGroup);
        repository.save(school);
        return school;
    }
    public void changeSchoolName(School school,String newSchoolName){
        school.setName(newSchoolName);
    }
*/
}
