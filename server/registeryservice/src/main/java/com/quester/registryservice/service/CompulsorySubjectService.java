package com.quester.registryservice.service;

//import com.quester.academic_records.model.*;

import java.util.ArrayList;
import java.util.List;
import java.util.Set;

public class CompulsorySubjectService {
   /* public void makeCompulsory(MakeCompusloryDto makeCompulsoryDto{
        Section section = makeCompulsoryDto.getSection();
        boolean allArms = makeCompulsoryDto.getAllArms()
        boolean allForms = makeCompulsoryDto.getAllForms()
        List<Form> forms = new ArrayList<>();
        List<Arm> arms= new ArrayList<>();
        Set<Subject> initialSubjects = makeCompulsoryDto.getSubjects();
        List<Subject> subjects = new ArrayList<>();
        subjects.addAll(initialSubjects);

        if(allArms){
           arms = armService.findArmBySectionId(sectionId);
        }
        else{
            Set<Arm> initialArms = makeCompulsoryDto.getArms();
            arms.addAll(initialForms);
        }

        if(allForms ){
             forms = formService.findArmBySectionId(sectionId);
        }
        else{
            Set<Form> initialForms = makeCompulsoryDto.getForms();
            forms.addAll(initialForms);
        }
        CompulsorySubject compulsorySubject = new CompulsorySubject(subjects,arms,forms);
      }

      public Subject removeCompulsorySubject(Set for Arm Form and Section){
        Section section = makeCompulsoryDto.getSection();
        boolean allArms = makeCompulsoryDto.getAllArms()
        boolean allForms = makeCompulsoryDto.getAllForms()
        List<Form> forms = new ArrayList<>();
        List<Arm> arms= new ArrayList<>();
        Set<Subject> initialSubjects = makeCompulsoryDto.getSubjects();
        List<Subject> subjects = new ArrayList<>();
        subjects.addAll(initialSubjects);

        if(allArms){
           arms = armService.findArmBySectionId(sectionId);
        }
        else{
            Set<Arm> initialArms = makeCompulsoryDto.getArms();
            arms.addAll(initialForms);
        }

        if(allForms ){
             forms = formService.findArmBySectionId(sectionId);
        }
        else{
            Set<Form> initialForms = makeCompulsoryDto.getForms();
            forms.addAll(initialForms);
        }
        //use a for loop through each of the forms and arms then get their compulsorysubject
        //from the repo and then remove the subject from its subject
    }*/
}
